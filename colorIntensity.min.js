function getIntensity(colors){var color=[];if(colors.charAt(0)=="#"){color=convertToRGB(colors)}else{var thenum=colors.match(/\d|,/g);console.log(thenum);color=thenum.join("").split(",")}let darkness=calculateDarkness(color);return[color,darkness]}function calculateDarkness(color){var roundedArr=[]for(var i=0;i<color.length;i++){let round=Math.round((color[i])/25);roundedArr.push(round)}return[Math.floor(roundedArr.reduce((a,b)=>a+b)/3),roundedArr]}function convertToRGB(color){var hex=color.substr(1);var regex=hex.match(/(.{2})/g)var values=[]regex.forEach(function(val,i){values.push(parseInt(val,16))});return values}